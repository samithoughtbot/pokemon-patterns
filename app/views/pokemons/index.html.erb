All Pokemons
<br>
<br>

<% @pokemons.each do |pokemon| %>
  <b>Name:</b> <%= pokemon.name %>
  <br>
  <b>Kind:</b> <%= pokemon.kind %>
  <br>
  <%= render(EvolveButtonComponent.new(pokemon: pokemon)) %>
  <br>
  <%= button_to 'Delete Pokemon', { action: 'destroy', id: pokemon.id }, method: :delete %>
  <br>
<% end %>

<%= link_to "Create new pokemon", new_pokemon_path %>